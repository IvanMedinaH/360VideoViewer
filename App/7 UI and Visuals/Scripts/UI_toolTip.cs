using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;
using UnityEngine.EventSystems;
using UnityEngine.UI;

public class UI_toolTip : Selectable //, IPointerEnterHandler
{
    /*
    public GameObject toolTip_ClickMe;
    public bool  isToolTipVisible;

    BaseEventData m_BaseEvent;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {

        if (IsHighlighted(m_BaseEvent) == true)
        {
            toolTip_ClickMe.SetActive(true);
        }
        if (IsHighlighted(m_BaseEvent) == false)
        {
            toolTip_ClickMe.SetActive(false);
        }
    }
    */
}
